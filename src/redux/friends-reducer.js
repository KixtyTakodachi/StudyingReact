let initalState = {
	friendsData: [
		{
			name: "Andrey",
		},
		{
			name: "Sasha",
		},
		{
			name: "Sveta",
		},
	],
};

const friendsReducer = (state = initalState, action) => {
	return state;
};

export default friendsReducer;
